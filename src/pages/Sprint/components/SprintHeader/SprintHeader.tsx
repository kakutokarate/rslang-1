import { FC } from "react";
import { Wrapper } from "./SprintHeader.styled";

const SprintHeader: FC = () => {
  return (
    <Wrapper>
      SprintHeader
    </Wrapper>
  );
};

export default SprintHeader;